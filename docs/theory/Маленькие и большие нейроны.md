### 1️⃣ Свёртка и карты признаков

* **Каждый фильтр (kernel) можно представить как маленький «нейрон»**, который смотрит на маленький участок изображения (например, 3×3 пикселя).
* Этот фильтр вычисляет сумму произведений входных пикселей на свои веса → выдаёт одно число для этого участка.
* Применяем фильтр ко всему изображению → получается **карта признаков** (feature map).

> Вроде как «нейрон в нейроне», потому что один маленький фильтр «сканирует» множество маленьких участков и создаёт свою карту.

---

### 2️⃣ Несколько фильтров → несколько карт признаков

* У тебя 8 фильтров → 8 карт признаков.
* Каждая карта — это «сигналы нейронов», которые реагируют на разные паттерны.
* Можно представить это как **слой нейронов**, каждый с собственной функцией активации (ReLU).

---

### 3️⃣ Пулинг и Flatten

* **MaxPool** выбирает самые сильные сигналы в каждом блоке карты признаков → как если бы «сильнейшие нейроны» выжили, а остальные отключились.
* **Flatten** объединяет все карты в один вектор → теперь все эти «выжившие нейроны» формируют единый набор признаков для Dense слоя.

---

### 4️⃣ Dense слой

* Dense слой — это уже настоящий «слой нейронов», где **каждый нейрон получает сигналы со всех карт признаков**.
* Логиты Dense слоя → новые «нейроны», которые комбинируют информацию от всех фильтров предыдущего слоя.

---

### 5️⃣ Почему создаётся ощущение «нейрон в нейроне»

1. Свёрточные фильтры реагируют локально, как маленькие нейроны → формируют карты признаков.
2. Каждая карта проходит через ReLU → отдельная активация.
3. MaxPool выбирает максимум → создаёт «выжившие» сигналы.
4. Flatten + Dense слой → снова объединяет эти сигналы → новые нейроны.

То есть визуально: **сначала много маленьких нейронов локально, потом они комбинируются в новый слой нейронов**, поэтому кажется, что нейроны «вырастают» друг из друга.

---

💡 Метафора:

* Свёртка = маленькие «детекторы паттернов»
* Пулинг = оставляем только сильные детекторы
* Dense = мозг, который соединяет все детекторы и делает решение
* На выходе создаётся новый набор нейронов, которые уже «смотрят» на всю картину целиком


---

### 1️⃣ Свёрточные фильтры — «маленькие нейроны»

Допустим, у нас **3 фильтра**, каждый 3×3, сканируют изображение 5×5:

```
Input Image 5x5
+---------+
| . . . . . |
| . . . . . |
| . . . . . |
| . . . . . |
| . . . . . |
+---------+

Filter 0 (3x3) --> Feature Map 0
Filter 1 (3x3) --> Feature Map 1
Filter 2 (3x3) --> Feature Map 2
```

* Каждый фильтр реагирует на свой паттерн
* Выдаёт карту признаков (feature map) → маленькие «нейроны», которые активируются на паттерн

---

### 2️⃣ ReLU + MaxPool — «выбор сильнейших нейронов»

```
Feature Map 0  Feature Map 1  Feature Map 2
   [1 2 0]        [0 1 2]       [1 0 3]
   [0 1 3]        [2 0 1]       [0 2 1]
   [1 0 2]        [1 3 0]       [2 1 0]

MaxPool 2x2 → оставляем только максимум из каждого блока
Feature Map 0: [2 3]  Feature Map 1: [2 2]  Feature Map 2: [3 3]
```

* Каждая карта признаков теперь уменьшена
* Только самые сильные сигналы («выжившие нейроны») проходят дальше

---

### 3️⃣ Flatten — собираем все маленькие нейроны в один ряд

```
Flattened vector = [F0_max1, F0_max2, F1_max1, F1_max2, F2_max1, F2_max2]
```

* Все «выжившие нейроны» с разных фильтров → один длинный вектор
* Подготавливает к Dense слою

---

### 4️⃣ Dense слой — новые нейроны, объединяющие информацию

```
Flat vector → Dense (например, 4 нейрона для примера)

         W[0][i]      W[1][i]      W[2][i]      W[3][i]
F0_max1 --*--> o0      *--> o1      *--> o2      *--> o3
F0_max2 --*--> o0      *--> o1      *--> o2      *--> o3
F1_max1 --*--> o0      *--> o1      *--> o2      *--> o3
F1_max2 --*--> o0      *--> o1      *--> o2      *--> o3
F2_max1 --*--> o0      *--> o1      *--> o2      *--> o3
F2_max2 --*--> o0      *--> o1      *--> o2      *--> o3
                  +b0        +b1        +b2        +b3
                  v          v          v          v
               logit[0]   logit[1]   logit[2]   logit[3]
```

* Каждый нейрон Dense слоя видит **все «выжившие нейроны» с фильтров**
* Вычисляет логит → сырая оценка класса

---

### 5️⃣ Метафора

* **Свёртка** → множество маленьких локальных нейронов, реагирующих на паттерны
* **MaxPool** → оставляет только самые сильные сигналы
* **Flatten + Dense** → новые нейроны, которые комбинируют все сильные сигналы, чтобы принять решение на уровне всего изображения
